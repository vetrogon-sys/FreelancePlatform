sudo: false

language: java
jdk: oraclejdk8
dist: trusty

install: true
script: mvn test
deploy:
  provider: heroku
  app: freelance-platform-8080
  api_key:
    secure: $HEROKU_API_KEY

branches:
  only:
    - master
